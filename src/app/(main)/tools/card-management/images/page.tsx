export default async function ImagesPage() {
    // const images: ImageProps[] = ((await getImages()) as ImageProps[]) || [];
    // console.log('Images', images);
    // return (
    //     <div>
    //         <div>
    //             <h1>Images - {images.length}</h1>
    //             {images.map((image) => (
    //                 <div key={image.id}>
    //                     <p>{image.public_id}</p>
    //                 </div>
    //             ))}
    //         </div>
    //     </div>
    // );
}
