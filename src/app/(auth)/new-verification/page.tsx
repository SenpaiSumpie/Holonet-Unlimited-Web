import { NewVerificationForm } from '@/modules/authentication/components/new-verification-form';

const NewVerificationPage = () => {
    return (
        <div className="w-full flex justify-center pt-8 pb-8">
            <NewVerificationForm />
        </div>
    );
};

export default NewVerificationPage;
